Create a simple page according to the picture. Sizes and colors are not reqired to be identical to the picture.

Requirements: 

1. Style rules for header and footer shouldn't be repeated. Use grouping. Only text-align property may be specified separately. 
2. Buttons should have same html code. They should not have id's or classes. Style of the button should be specified according to where they are placed. if you move a button from header to section, it should look as other buttons in section (brown background).
3. "Subscibe" button should look differently. (Use direct child selector).

Note: you will need to use next css properties: padding, background-color, color, border, margin.

